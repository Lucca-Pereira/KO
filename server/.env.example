# Copy to .env and fill in. Never commit the real .env.

# Shared bearer token the phone and Claude Desktop's MCP config both send. Any long random
# string works, e.g. `openssl rand -hex 32`. Leave unset only for local testing off the NAS.
KO_API_TOKEN=
